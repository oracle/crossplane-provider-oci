
apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: oci-logging-namespaced-v1alpha1-tests-template
spec:
  serviceAccountName: argo-workflow
  entrypoint: logging-namespaced-tests-workflow
  volumes:
    - name: git-repo
      persistentVolumeClaim:
        claimName: git-repo-pvc
  arguments:
    parameters:
    - name: target_namespace
    - name: compartment_ocid
    - name: objectstorage_namespace
    - name: create_compartment
      value: "true"
    - name: create_object
      value: "true"
    - name: create_resources
      value: "true"
    - name: delete_resources
      value: "true"
  templates:
    - name: logging-namespaced-tests-workflow
      inputs:
        parameters:
          - name: target_namespace
            value: "{{workflow.parameters.target_namespace}}"
      dag:
        tasks:
        - name: create-compartment
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_compartment}} == true"
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/identity/v1alpha1/compartment.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
              - name: envVars
                value: "COMPARTMENT_OCID={{workflow.parameters.compartment_ocid}}"
        - name: create-object
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_object}} == true"
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/objectstorage/v1alpha1/object.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
              - name: envVars
                value: "OBJECTSTORAGE_NAMESPACE={{workflow.parameters.objectstorage_namespace}}"
        - name: create-log
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-loggroup]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/logging/v1alpha1/log.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
              - name: envVars
                value: "COMPARTMENT_OCID={{workflow.parameters.compartment_ocid}}"
        - name: create-customlog
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-loggroup]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/logging/v1alpha1/customlog.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: create-loggroup
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-compartment]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/logging/v1alpha1/loggroup.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: create-logsavedsearch
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-compartment]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/logging/v1alpha1/logsavedsearch.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: create-unifiedagentconfiguration
          templateRef:
            name: test-template
            template: create-resource
          when: "{{workflow.parameters.create_resources}} == true"
          dependencies: [create-compartment, create-object]
          arguments:
            parameters:
              - name: resourceFile
                value: "examples/namespaced/logging/v1alpha1/unifiedagentconfiguration.yaml"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: delete-log
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-log"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-log.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-log.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-log.outputs.parameters.resourceGroup}}"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: delete-customlog
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-customlog"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-customlog.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-customlog.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-customlog.outputs.parameters.resourceGroup}}"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: delete-loggroup
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-loggroup", "delete-customlog", "delete-log"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-loggroup.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-loggroup.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-loggroup.outputs.parameters.resourceGroup}}"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: delete-logsavedsearch
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-logsavedsearch"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-logsavedsearch.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-logsavedsearch.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-logsavedsearch.outputs.parameters.resourceGroup}}"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
        - name: delete-unifiedagentconfiguration
          templateRef:
            name: test-template
            template: delete-resource
          when: "{{workflow.parameters.delete_resources}} == true"
          dependencies: ["create-unifiedagentconfiguration"]
          arguments:
            parameters:
              - name: resourceKind
                value: "{{tasks.create-unifiedagentconfiguration.outputs.parameters.resourceKind}}"
              - name: resourceName
                value: "{{tasks.create-unifiedagentconfiguration.outputs.parameters.resourceName}}"
              - name: resourceGroup
                value: "{{tasks.create-unifiedagentconfiguration.outputs.parameters.resourceGroup}}"
              - name: namespace
                value: "{{inputs.parameters.target_namespace}}"
