apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: oci-crossplane-test-
spec:
  serviceAccountName: argo-workflow
  entrypoint: crossplane-provider-oci-test-workflow
  volumes:
    - name: git-repo
      persistentVolumeClaim:
        claimName: git-repo-pvc
  arguments:
    parameters:
    - name: run_apigateway_namespaced_tests
      value: "false"
    - name: run_apm_namespaced_tests
      value: "false"
    - name: run_artifacts_namespaced_tests
      value: "false"
    - name: run_autoscaling_namespaced_tests
      value: "false"
    - name: run_bastion_namespaced_tests
      value: "false"
    - name: run_blockstorage_namespaced_tests
      value: "false"
    - name: run_budget_namespaced_tests
      value: "false"
    - name: run_certificatesmanagement_namespaced_tests
      value: "false"
    - name: run_compute_namespaced_tests
      value: "false"
    - name: run_containerengine_namespaced_tests
      value: "false"
    - name: run_datacatalog_namespaced_tests
      value: "false"
    - name: run_dataintegration_namespaced_tests
      value: "false"
    - name: run_dns_namespaced_tests
      value: "false"
    - name: run_email_namespaced_tests
      value: "false"
    - name: run_events_namespaced_tests
      value: "false"
    - name: run_filestorage_namespaced_tests
      value: "false"
    - name: run_functions_namespaced_tests
      value: "false"
    - name: run_healthchecks_namespaced_tests
      value: "false"
    - name: run_identity_namespaced_tests
      value: "false"
    - name: run_integration_namespaced_tests
      value: "false"
    - name: run_kms_namespaced_tests
      value: "false"
    - name: run_limits_namespaced_tests
      value: "false"
    - name: run_loadbalancer_namespaced_tests
      value: "false"
    - name: run_logging_namespaced_tests
      value: "false"
    - name: run_mediaservices_namespaced_tests
      value: "false"
    - name: run_monitoring_namespaced_tests
      value: "false"
    - name: run_mysql_namespaced_tests
      value: "false"
    - name: run_networking_namespaced_tests
      value: "false"
    - name: run_networkloadbalancer_namespaced_tests
      value: "false"
    - name: run_nosql_namespaced_tests
      value: "false"
    - name: run_objectstorage_namespaced_tests
      value: "false"
    - name: run_ons_namespaced_tests
      value: "false"
    - name: run_psql_namespaced_tests
      value: "false"
    - name: run_queue_namespaced_tests
      value: "false"
    - name: run_recovery_namespaced_tests
      value: "false"
    - name: run_redis_namespaced_tests
      value: "false"
    - name: run_resourcemanager_namespaced_tests
      value: "false"
    - name: run_streaming_namespaced_tests
      value: "false"
    - name: run_vault_namespaced_tests
      value: "false"
    - name: run_waf_namespaced_tests
      value: "false"
    - name: availability_domain
      value: ""
    - name: boot_volume_source_id
      value: ""
    - name: compartment_ocid
      value: ""
    - name: computehost_id
      value: ""
    - name: create_application
      value: "true"
    - name: create_compartment
      value: "true"
    - name: create_function
      value: "true"
    - name: create_image
      value: "true"
    - name: create_instance
      value: "true"
    - name: create_key
      value: "true"
    - name: create_keyversion
      value: "true"
    - name: create_loadbalancer
      value: "true"
    - name: create_notificationtopic
      value: "true"
    - name: create_object
      value: "true"
    - name: create_resources
      value: "true"
    - name: create_secret
      value: "true"
    - name: create_subnet
      value: "true"
    - name: create_vault
      value: "true"
    - name: create_vcn
      value: "true"
    - name: delete_resources
      value: "true"
    - name: function_image
      value: ""
    - name: image_instance_ocid
      value: ""
    - name: instanceconsoleconnection_publickey
      value: ""
    - name: instancemaintenanceevent_id
      value: ""
    - name: instancepool_ocid
      value: ""
    - name: namespace_list
      value: "[]"
    - name: nosql_table_ddlstatement
      value: ""
    - name: objectstorage_namespace
      value: ""
    - name: ssh_public_key
      value: ""
    - name: subnet_ocid
      value: ""
    - name: target_resource_ocid
      value: ""
    - name: tenancy_ocid
      value: ""
    - name: usageplan_ocid
      value: ""
    - name: vault_crypto_endpoint
      value: ""
    - name: vault_key_signature
      value: ""
    - name: vault_management_endpoint
      value: ""
  templates:
    - name: crossplane-provider-oci-test-workflow
      dag:
        tasks:
        - name: apigateway-namespaced-tests
          templateRef:
            name: oci-apigateway-namespaced-v1alpha1-tests-template
            template: apigateway-namespaced-tests-workflow
          when: "{{workflow.parameters.run_apigateway_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: usageplan_ocid
                value: "{{workflow.parameters.usageplan_ocid}}"
        - name: apm-namespaced-tests
          templateRef:
            name: oci-apm-namespaced-v1alpha1-tests-template
            template: apm-namespaced-tests-workflow
          when: "{{workflow.parameters.run_apm_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: artifacts-namespaced-tests
          templateRef:
            name: oci-artifacts-namespaced-v1alpha1-tests-template
            template: artifacts-namespaced-tests-workflow
          when: "{{workflow.parameters.run_artifacts_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_image
                value: "{{workflow.parameters.create_image}}"
              - name: create_key
                value: "{{workflow.parameters.create_key}}"
              - name: create_keyversion
                value: "{{workflow.parameters.create_keyversion}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: image_instance_ocid
                value: "{{workflow.parameters.image_instance_ocid}}"
              - name: vault_key_signature
                value: "{{workflow.parameters.vault_key_signature}}"
              - name: vault_management_endpoint
                value: "{{workflow.parameters.vault_management_endpoint}}"
        - name: autoscaling-namespaced-tests
          templateRef:
            name: oci-autoscaling-namespaced-v1alpha1-tests-template
            template: autoscaling-namespaced-tests-workflow
          when: "{{workflow.parameters.run_autoscaling_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: instancepool_ocid
                value: "{{workflow.parameters.instancepool_ocid}}"
        - name: bastion-namespaced-tests
          templateRef:
            name: oci-bastion-namespaced-v1alpha1-tests-template
            template: bastion-namespaced-tests-workflow
          when: "{{workflow.parameters.run_bastion_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: ssh_public_key
                value: "{{workflow.parameters.ssh_public_key}}"
              - name: target_resource_ocid
                value: "{{workflow.parameters.target_resource_ocid}}"
        - name: blockstorage-namespaced-tests
          templateRef:
            name: oci-blockstorage-namespaced-v1alpha1-tests-template
            template: blockstorage-namespaced-tests-workflow
          when: "{{workflow.parameters.run_blockstorage_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: availability_domain
                value: "{{workflow.parameters.availability_domain}}"
              - name: boot_volume_source_id
                value: "{{workflow.parameters.boot_volume_source_id}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_image
                value: "{{workflow.parameters.create_image}}"
              - name: create_instance
                value: "{{workflow.parameters.create_instance}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: image_instance_ocid
                value: "{{workflow.parameters.image_instance_ocid}}"
        - name: budget-namespaced-tests
          templateRef:
            name: oci-budget-namespaced-v1alpha1-tests-template
            template: budget-namespaced-tests-workflow
          when: "{{workflow.parameters.run_budget_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: tenancy_ocid
                value: "{{workflow.parameters.tenancy_ocid}}"
        - name: certificatesmanagement-namespaced-tests
          templateRef:
            name: oci-certificatesmanagement-namespaced-v1alpha1-tests-template
            template: certificatesmanagement-namespaced-tests-workflow
          when: "{{workflow.parameters.run_certificatesmanagement_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_key
                value: "{{workflow.parameters.create_key}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: vault_management_endpoint
                value: "{{workflow.parameters.vault_management_endpoint}}"
        - name: compute-namespaced-tests
          templateRef:
            name: oci-compute-namespaced-v1alpha1-tests-template
            template: compute-namespaced-tests-workflow
          when: "{{workflow.parameters.run_compute_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: availability_domain
                value: "{{workflow.parameters.availability_domain}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: computehost_id
                value: "{{workflow.parameters.computehost_id}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: image_instance_ocid
                value: "{{workflow.parameters.image_instance_ocid}}"
              - name: instanceconsoleconnection_publickey
                value: "{{workflow.parameters.instanceconsoleconnection_publickey}}"
              - name: instancemaintenanceevent_id
                value: "{{workflow.parameters.instancemaintenanceevent_id}}"
        - name: containerengine-namespaced-tests
          templateRef:
            name: oci-containerengine-namespaced-v1alpha1-tests-template
            template: containerengine-namespaced-tests-workflow
          when: "{{workflow.parameters.run_containerengine_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: availability_domain
                value: "{{workflow.parameters.availability_domain}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_image
                value: "{{workflow.parameters.create_image}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: image_instance_ocid
                value: "{{workflow.parameters.image_instance_ocid}}"
        - name: datacatalog-namespaced-tests
          templateRef:
            name: oci-datacatalog-namespaced-v1alpha1-tests-template
            template: datacatalog-namespaced-tests-workflow
          when: "{{workflow.parameters.run_datacatalog_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: dataintegration-namespaced-tests
          templateRef:
            name: oci-dataintegration-namespaced-v1alpha1-tests-template
            template: dataintegration-namespaced-tests-workflow
          when: "{{workflow.parameters.run_dataintegration_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: dns-namespaced-tests
          templateRef:
            name: oci-dns-namespaced-v1alpha1-tests-template
            template: dns-namespaced-tests-workflow
          when: "{{workflow.parameters.run_dns_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: email-namespaced-tests
          templateRef:
            name: oci-email-namespaced-v1alpha1-tests-template
            template: email-namespaced-tests-workflow
          when: "{{workflow.parameters.run_email_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: tenancy_ocid
                value: "{{workflow.parameters.tenancy_ocid}}"
        - name: events-namespaced-tests
          templateRef:
            name: oci-events-namespaced-v1alpha1-tests-template
            template: events-namespaced-tests-workflow
          when: "{{workflow.parameters.run_events_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_application
                value: "{{workflow.parameters.create_application}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_function
                value: "{{workflow.parameters.create_function}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: function_image
                value: "{{workflow.parameters.function_image}}"
        - name: filestorage-namespaced-tests
          templateRef:
            name: oci-filestorage-namespaced-v1alpha1-tests-template
            template: filestorage-namespaced-tests-workflow
          when: "{{workflow.parameters.run_filestorage_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: availability_domain
                value: "{{workflow.parameters.availability_domain}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_key
                value: "{{workflow.parameters.create_key}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_secret
                value: "{{workflow.parameters.create_secret}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vault
                value: "{{workflow.parameters.create_vault}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: vault_management_endpoint
                value: "{{workflow.parameters.vault_management_endpoint}}"
        - name: functions-namespaced-tests
          templateRef:
            name: oci-functions-namespaced-v1alpha1-tests-template
            template: functions-namespaced-tests-workflow
          when: "{{workflow.parameters.run_functions_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: function_image
                value: "{{workflow.parameters.function_image}}"
        - name: healthchecks-namespaced-tests
          templateRef:
            name: oci-healthchecks-namespaced-v1alpha1-tests-template
            template: healthchecks-namespaced-tests-workflow
          when: "{{workflow.parameters.run_healthchecks_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: identity-namespaced-tests
          templateRef:
            name: oci-identity-namespaced-v1alpha1-tests-template
            template: identity-namespaced-tests-workflow
          when: "{{workflow.parameters.run_identity_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: integration-namespaced-tests
          templateRef:
            name: oci-integration-namespaced-v1alpha1-tests-template
            template: integration-namespaced-tests-workflow
          when: "{{workflow.parameters.run_integration_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: kms-namespaced-tests
          templateRef:
            name: oci-kms-namespaced-v1alpha1-tests-template
            template: kms-namespaced-tests-workflow
          when: "{{workflow.parameters.run_kms_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: vault_crypto_endpoint
                value: "{{workflow.parameters.vault_crypto_endpoint}}"
              - name: vault_management_endpoint
                value: "{{workflow.parameters.vault_management_endpoint}}"
        - name: limits-namespaced-tests
          templateRef:
            name: oci-limits-namespaced-v1alpha1-tests-template
            template: limits-namespaced-tests-workflow
          when: "{{workflow.parameters.run_limits_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: tenancy_ocid
                value: "{{workflow.parameters.tenancy_ocid}}"
        - name: loadbalancer-namespaced-tests
          templateRef:
            name: oci-loadbalancer-namespaced-v1alpha1-tests-template
            template: loadbalancer-namespaced-tests-workflow
          when: "{{workflow.parameters.run_loadbalancer_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: logging-namespaced-tests
          templateRef:
            name: oci-logging-namespaced-v1alpha1-tests-template
            template: logging-namespaced-tests-workflow
          when: "{{workflow.parameters.run_logging_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_object
                value: "{{workflow.parameters.create_object}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: objectstorage_namespace
                value: "{{workflow.parameters.objectstorage_namespace}}"
        - name: mediaservices-namespaced-tests
          templateRef:
            name: oci-mediaservices-namespaced-v1alpha1-tests-template
            template: mediaservices-namespaced-tests-workflow
          when: "{{workflow.parameters.run_mediaservices_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: monitoring-namespaced-tests
          templateRef:
            name: oci-monitoring-namespaced-v1alpha1-tests-template
            template: monitoring-namespaced-tests-workflow
          when: "{{workflow.parameters.run_monitoring_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_notificationtopic
                value: "{{workflow.parameters.create_notificationtopic}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: mysql-namespaced-tests
          templateRef:
            name: oci-mysql-namespaced-v1alpha1-tests-template
            template: mysql-namespaced-tests-workflow
          when: "{{workflow.parameters.run_mysql_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: availability_domain
                value: "{{workflow.parameters.availability_domain}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: networking-namespaced-tests
          templateRef:
            name: oci-networking-namespaced-v1alpha1-tests-template
            template: networking-namespaced-tests-workflow
          when: "{{workflow.parameters.run_networking_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: availability_domain
                value: "{{workflow.parameters.availability_domain}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_image
                value: "{{workflow.parameters.create_image}}"
              - name: create_instance
                value: "{{workflow.parameters.create_instance}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: image_instance_ocid
                value: "{{workflow.parameters.image_instance_ocid}}"
        - name: networkloadbalancer-namespaced-tests
          templateRef:
            name: oci-networkloadbalancer-namespaced-v1alpha1-tests-template
            template: networkloadbalancer-namespaced-tests-workflow
          when: "{{workflow.parameters.run_networkloadbalancer_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: nosql-namespaced-tests
          templateRef:
            name: oci-nosql-namespaced-v1alpha1-tests-template
            template: nosql-namespaced-tests-workflow
          when: "{{workflow.parameters.run_nosql_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: nosql_table_ddlstatement
                value: "{{workflow.parameters.nosql_table_ddlstatement}}"
        - name: objectstorage-namespaced-tests
          templateRef:
            name: oci-objectstorage-namespaced-v1alpha1-tests-template
            template: objectstorage-namespaced-tests-workflow
          when: "{{workflow.parameters.run_objectstorage_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: objectstorage_namespace
                value: "{{workflow.parameters.objectstorage_namespace}}"
        - name: ons-namespaced-tests
          templateRef:
            name: oci-ons-namespaced-v1alpha1-tests-template
            template: ons-namespaced-tests-workflow
          when: "{{workflow.parameters.run_ons_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: psql-namespaced-tests
          templateRef:
            name: oci-psql-namespaced-v1alpha1-tests-template
            template: psql-namespaced-tests-workflow
          when: "{{workflow.parameters.run_psql_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: queue-namespaced-tests
          templateRef:
            name: oci-queue-namespaced-v1alpha1-tests-template
            template: queue-namespaced-tests-workflow
          when: "{{workflow.parameters.run_queue_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: recovery-namespaced-tests
          templateRef:
            name: oci-recovery-namespaced-v1alpha1-tests-template
            template: recovery-namespaced-tests-workflow
          when: "{{workflow.parameters.run_recovery_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: subnet_ocid
                value: "{{workflow.parameters.subnet_ocid}}"
        - name: redis-namespaced-tests
          templateRef:
            name: oci-redis-namespaced-v1alpha1-tests-template
            template: redis-namespaced-tests-workflow
          when: "{{workflow.parameters.run_redis_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: resourcemanager-namespaced-tests
          templateRef:
            name: oci-resourcemanager-namespaced-v1alpha1-tests-template
            template: resourcemanager-namespaced-tests-workflow
          when: "{{workflow.parameters.run_resourcemanager_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
        - name: streaming-namespaced-tests
          templateRef:
            name: oci-streaming-namespaced-v1alpha1-tests-template
            template: streaming-namespaced-tests-workflow
          when: "{{workflow.parameters.run_streaming_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_key
                value: "{{workflow.parameters.create_key}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: vault_management_endpoint
                value: "{{workflow.parameters.vault_management_endpoint}}"
        - name: vault-namespaced-tests
          templateRef:
            name: oci-vault-namespaced-v1alpha1-tests-template
            template: vault-namespaced-tests-workflow
          when: "{{workflow.parameters.run_vault_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_key
                value: "{{workflow.parameters.create_key}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_vault
                value: "{{workflow.parameters.create_vault}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
              - name: vault_management_endpoint
                value: "{{workflow.parameters.vault_management_endpoint}}"
        - name: waf-namespaced-tests
          templateRef:
            name: oci-waf-namespaced-v1alpha1-tests-template
            template: waf-namespaced-tests-workflow
          when: "{{workflow.parameters.run_waf_namespaced_tests}} == true"
          withParam: "{{workflow.parameters.namespace_list}}"
          arguments:
            parameters:
              - name: target_namespace
                value: "{{item}}"
              - name: compartment_ocid
                value: "{{workflow.parameters.compartment_ocid}}"
              - name: create_compartment
                value: "{{workflow.parameters.create_compartment}}"
              - name: create_loadbalancer
                value: "{{workflow.parameters.create_loadbalancer}}"
              - name: create_resources
                value: "{{workflow.parameters.create_resources}}"
              - name: create_subnet
                value: "{{workflow.parameters.create_subnet}}"
              - name: create_vcn
                value: "{{workflow.parameters.create_vcn}}"
              - name: delete_resources
                value: "{{workflow.parameters.delete_resources}}"
