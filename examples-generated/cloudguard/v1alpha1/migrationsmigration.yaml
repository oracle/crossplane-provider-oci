apiVersion: cloudguard.oci.upbound.io/v1alpha1
kind: MigrationsMigration
metadata:
  annotations:
    meta.upbound.io/example-id: cloudguard/v1alpha1/migrationsmigration
  labels:
    testing.upbound.io/example-name: test_migration
  name: test-migration
spec:
  forProvider:
    compartmentId: ${var.compartment_id}
    definedTags:
      foo-namespace.bar-key: value
    displayName: ${var.migration_display_name}
    freeformTags:
      bar-key: value
    isCompleted: ${var.migration_is_completed}
    replicationScheduleId: ${oci_cloud_migrations_replication_schedule.test_replication_schedule.id}
