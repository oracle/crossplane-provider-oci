apiVersion: cloudguard.oci.upbound.io/v1alpha1
kind: BridgeDiscoverySchedule
metadata:
  annotations:
    meta.upbound.io/example-id: cloudguard/v1alpha1/bridgediscoveryschedule
  labels:
    testing.upbound.io/example-name: test_discovery_schedule
  name: test-discovery-schedule
spec:
  forProvider:
    compartmentId: ${var.compartment_id}
    definedTags:
      Operations.CostCenter: "42"
    displayName: ${var.discovery_schedule_display_name}
    executionRecurrences: ${var.discovery_schedule_execution_recurrences}
    freeformTags:
      Department: Finance
