apiVersion: cloudguard.oci.upbound.io/v1alpha1
kind: Recommendation
metadata:
  annotations:
    meta.upbound.io/example-id: cloudguard/v1alpha1/recommendation
  labels:
    testing.upbound.io/example-name: test_recommendation
  name: test-recommendation
spec:
  forProvider:
    recommendationId: ${oci_optimizer_recommendation.test_recommendation.id}
    status: ${var.recommendation_status}
    timeStatusEnd: ${var.recommendation_time_status_end}
